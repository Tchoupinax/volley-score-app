export type Set = {
  createdAt: string;
  externalTeamScore: number;
  finishedAt?: string;
  gameId: string;
  homeTeamScore: number;
  id: string;
  updatedAt: string;
  setPosition: number;
}
